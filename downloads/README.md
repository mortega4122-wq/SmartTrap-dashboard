# Downloads

PDFs linked from `docs.html` go in this folder. Each card on the Documentation page has its expected file name in a `data-file` attribute (for example `downloads/assembly-guide.pdf`). After adding a file, replace that card's disabled button with a real link, as described in the comment at the top of `docs.html`.
